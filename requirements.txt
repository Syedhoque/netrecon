# ===================================================================
# NetRecon - Security & Reconnaissance Suite
# Dependencies Requirements File
# ===================================================================

# HTTP Requests library for Directory Brute-forcing engine
requests>=2.28.0

# Note: Tkinter, socket, threading, and concurrent.futures 
# are part of the Python Standard Library. 
# On Linux (Kali/Ubuntu), install Tkinter via system package manager:
# sudo apt-get install python3-tk
